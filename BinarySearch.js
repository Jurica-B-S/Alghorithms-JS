'use strict';

//use for array of numbers

function binarySearch(array){
  let high = array.length - 1;
  let low = 0;
  
  if(!Array.isArray(array)){
  trow("Wrong file type - send an array of numbers")
  }
  
  
}
